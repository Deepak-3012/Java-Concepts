
public class Keywords {
    public static void main(String[] args){}
    //reserved words in java--> 53+ is avaialble

    // 53 are commonly used

    // out of 53 there are 3 called as  reserved LITERAL(fixed value) and others are keyword

    //--> reseverd Literal(true, false, null)

    //in the remaining 50 --> 2 keyword are unused(not meant to be used)

    //--> unused(goto and const)

    //-->other 48 are used keyword, will list it below
    //----------------------------------------------------------------------------
    // primitive keyword-->byte,short,int,long,float,double,boolean and char

    //control flow keyword--> if,else,switch,break,continue,return,for,while,do,default,case

    /*modifier keyword--> public, private, protected, static, final, transient, volatile,
    abstract,native, synchronized, strictfp(1.2 VERSION)
     */

    //exception handeling-->try, catch, finally, assert(1.4 VERSION), throw, throws

    //class related -->extends, implements, class, interface, import, package

    /*object related--> new, this, super,instanceof [NOTE: WE DON'T HAVE DELETE KEYWORD,
     CUZ IT IS
    HANDLED BY GARBAGE COLLECTION*/

    // return type : void

    /*enum(1.5 VERSION) used to define group named constant
    example:
    enum month
    {Jan, Feb , march}
     */

    // these are the 53 keywords including the reserved literal

    //all 53 will use only lower case alphabets !!!

}
